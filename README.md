# Brazilian Pizzas Menu

Estrutura recriada a partir do conteúdo enviado na conversa.

## Importante
As imagens reais não foram incluídas neste ZIP porque você enviou apenas os nomes dos arquivos, não os arquivos de imagem.

## Pastas esperadas
- `Menu_PT/` com `PT_00.jpeg` até `PT_11.jpeg`
- `Menu_EN/` com `EN_00.jpeg` até `EN_11.jpeg`

## Observação
Atualizei `script.js` e `images.json` para incluir as páginas `00`, já que elas estavam listadas nas pastas mas não estavam referenciadas no código enviado.
